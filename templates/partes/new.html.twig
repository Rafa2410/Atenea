{% extends 'base.html.twig' %}

{% block title %}Nueva parte interesada{% endblock %}

{% block body %}
    <h3>Crear parte interesada</h3>

    {{ form_start(form) }}    
        {{ form_row(form.nombre) }}
        {#{{ form_row(form.unidad_de_gestion) }}#}
    <br>
    <label>Tipo</label>
    <div class="input-field col s6">
        {{ form_row(form.tipoPartesInteresadas) }}
    </div>
    <br>
    <label>Expectativa</label>
    <div class="input-field col s6">
        {{ form_row(form.expectativaPartesInteresadas) }}
    </div>
    <button class="btn waves-effect waves-light right" type="submit">Añadir</button>
    <a href="#" onClick="window.history.back();" class="btn red darken-1">Volver</a>
    <br/>
    {{ form_end(form) }}

{% endblock %}

{% block javascripts %}
    <script type="text/javascript">
    $(document).ready(function(){
        $('select').formSelect();
        $('#partes_interesadas_tipoPartesInteresadas').attr('style','display: none !important');
        $('#partes_interesadas_expectativaPartesInteresadas').attr('style','display: none !important');
    });
    </script>
{% endblock %}
