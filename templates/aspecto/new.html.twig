{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
        <h3>{{ title }}</h3>
    {{ form_start(form) }}
    {{ form_row(form.descripcion) }}
    <div class="input-field col s12">
        {{ form_row(form.cuestiones) }}
    </div>
    <br/>
    <button class="btn waves-effect waves-light right" type="submit">Añadir</button>
    <button type="submit" class="btn btn-lg btn-primary red" onClick="javascript:history.back()"><a style="color:white;">Cancelar</a>
    {{ form_end(form) }}

{% endblock %}
{% block javascripts %}    
    <script>
    $(document).ready(function(){
        $('select').formSelect();
        $('#aspecto_cuestiones').attr('style','display: none !important');
    });
    </script>
{% endblock %}