{% extends 'base.html.twig' %}

{% block title %}Hello CuestionExternaController!{% endblock %}

{% block body %}
    <div class="container">
        <h3>Editar cuestion externa</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        {%for message in app.flashes('notice') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
    </div>
{% endblock %}
